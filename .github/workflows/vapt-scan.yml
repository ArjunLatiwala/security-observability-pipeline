name: Step 4 & 5 - Final Scalable Enterprise Architecture

on:
  workflow_dispatch:

# 🌍 GLOBAL CONFIGURATION
env:
  TARGET_URL: 'https://husaintrivedi.site'
  LATENCY_THRESHOLD: '10' # Forces failure for testing Sentry/Audit
  WAZUH_INDEXER_URL: 'https://35.200.183.81:8080' # Using remapped port 8080

jobs:
  # ==========================================
  # JOB 1: STEP 4 - Deep Security (VAPT)
  # ==========================================
  security_vapt_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: OWASP ZAP Full Scan
        uses: zaproxy/action-full-scan@v0.12.0
        with:
          target: ${{ env.TARGET_URL }}
          fail_action: false
          cmd_options: '-a -j -r report_html.html -J report_json.json'

      # ✅ LOG SECURITY AUDIT TO MASTER DATABASE (Port 8080)
      - name: Log Security Audit to Wazuh
        if: always()
        continue-on-error: true
        run: |
          curl -k -X POST "${{ env.WAZUH_INDEXER_URL }}/pipeline-audit/_doc/" \
          -u "${{ secrets.WAZUH_API_USER }}:${{ secrets.WAZUH_API_PASSWORD }}" \
          -H "Content-Type: application/json" \
          -d '{
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "event_type": "SECURITY_VAPT_SCAN",
            "product_id": "${{ secrets.DEFECTDOJO_PRODUCT_ID }}",
            "repo": "${{ github.event.repository.name }}",
            "status": "${{ job.status }}",
            "message": "ZAP Scan completed for ${{ env.TARGET_URL }}",
            "run_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }'

      - name: Upload Security Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vapt-reports
          path: |
            report_html.html
            report_json.json

  # ==========================================
  # JOB 2: STEP 5 - Benchmarking (Stress & Load)
  # ==========================================
  benchmarking_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code 
        uses: actions/checkout@v4

      - name: Setup Performance Tools
        run: |
          sudo apt-get update && sudo apt-get install -y apache2-utils jq
          curl -fsSL https://dl.k6.io/key.gpg | sudo gpg --dearmor -o /usr/share/keyrings/k6-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update && sudo apt-get install -y k6

      - name: Run Apache Bench Stress Test
        run: ab -n 500 -c 10 ${{ env.TARGET_URL }}/ | tee stress-results.txt

      - name: Run k6 Load Test
        run: |
          cat << EOF > k6-test.js
          import http from 'k6/http';
          import { sleep, check } from 'k6';
          export const options = { 
            vus: 10, duration: '10s',
            thresholds: { 'http_req_duration': ['p(98)<$THRESHOLD'] } 
          };
          export default function () {
            let res = http.get('$URL'); 
            check(res, { 'status is 200': (r) => r.status === 200 });
            sleep(1);
          }
          EOF
          k6 run --out cloud --tag product_id="${{ secrets.DEFECTDOJO_PRODUCT_ID }}" k6-test.js
        env:
          THRESHOLD: ${{ env.LATENCY_THRESHOLD }}
          URL: ${{ env.TARGET_URL }}
          K6_CLOUD_TOKEN: ${{ secrets.K6_CLOUD_TOKEN }}
          K6_CLOUD_PROJECT_ID: 7324608
          K6_CLOUD_NAME: "${{ github.event.repository.name }}"
          
      # ✅ LOG PERFORMANCE AUDIT TO MASTER DATABASE (Port 8080)
      - name: Log Performance Audit to Wazuh
        if: always()
        continue-on-error: true
        run: |
          curl -k -X POST "${{ env.WAZUH_INDEXER_URL }}/pipeline-audit/_doc/" \
          -u "${{ secrets.WAZUH_API_USER }}:${{ secrets.WAZUH_API_PASSWORD }}" \
          -H "Content-Type: application/json" \
          -d '{
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "event_type": "PERFORMANCE_BENCHMARK",
            "product_id": "${{ secrets.DEFECTDOJO_PRODUCT_ID }}",
            "repo": "${{ github.event.repository.name }}",
            "status": "${{ job.status }}",
            "threshold": "${{ env.LATENCY_THRESHOLD }}ms",
            "run_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }'

      - name: Report Failure to Sentry
        if: failure()
        env:
          THRESHOLD: ${{ env.LATENCY_THRESHOLD }}
          URL: ${{ env.TARGET_URL }}
        run: |
          DSN="${{ secrets.SENTRY_DSN }}"
          SENTRY_KEY=$(echo $DSN | awk -F'[:@/]' '{print $4}')
          SENTRY_HOST=$(echo $DSN | awk -F'[:@/]' '{print $5}')
          PROJECT_ID=$(echo $DSN | awk -F'[:@/]' '{print $6}')
      
          curl -X POST "https://$SENTRY_HOST/api/$PROJECT_ID/store/" \
          -H "X-Sentry-Auth: Sentry sentry_version=7, sentry_key=$SENTRY_KEY, sentry_client=curl/1.0" \
          -H 'Content-Type: application/json' \
          -d '{
            "message": "Performance Regression: ${{ github.event.repository.name }}",
            "exception": {
              "values": [{
                "type": "PerformanceThresholdError",
                "value": "Application at '$URL' failed latency threshold of '$THRESHOLD'ms for Product ID: ${{ secrets.DEFECTDOJO_PRODUCT_ID }}."
              }]
            },
            "level": "error",
            "fingerprint": ["${{ github.event.repository.name }}", "perf-failure"],
            "tags": {
              "repo": "${{ github.event.repository.name }}",
              "product_id": "${{ secrets.DEFECTDOJO_PRODUCT_ID }}"
            }
          }'

      - name: Upload Benchmarking Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: performance-logs
          path: |
            stress_results.txt
            k6-test.js
